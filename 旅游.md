---
layout: page
title: 旅游
---

<div class="posts">
  {% comment %} 置顶：走过的国家、走过的省份 {% endcomment %}
  {% for post in site.posts %}
    {% if post.category == "旅游" and post.title contains "走过的国家" %}
    <div class="readme-item">
      <div class="readme-item-header">
        <h2 class="readme-item-title">
          <a href="{{ post.url | relative_url }}">{{ post.title }}</a>
        </h2>
        <span class="readme-item-date">{{ post.date | date: "%Y-%m-%d" }}</span>
      </div>
    </div>
    {% endif %}
  {% endfor %}
  {% for post in site.posts %}
    {% if post.category == "旅游" and post.title contains "走过的省份" %}
    <div class="readme-item">
      <div class="readme-item-header">
        <h2 class="readme-item-title">
          <a href="{{ post.url | relative_url }}">{{ post.title }}</a>
        </h2>
        <span class="readme-item-date">{{ post.date | date: "%Y-%m-%d" }}</span>
      </div>
    </div>
    {% endif %}
  {% endfor %}
  {% comment %} 其余推文按日期排序 {% endcomment %}
  {% for post in site.posts %}
    {% assign is_pinned = false %}
    {% if post.title contains "走过的国家" or post.title contains "走过的省份" %}{% assign is_pinned = true %}{% endif %}
    {% if post.category == "旅游" and is_pinned == false %}
    <div class="readme-item">
      <div class="readme-item-header">
        <h2 class="readme-item-title">
          <a href="{{ post.url | relative_url }}">{{ post.title }}</a>
        </h2>
        <span class="readme-item-date">{{ post.date | date: "%Y-%m-%d" }}</span>
      </div>
    </div>
    {% endif %}
  {% endfor %}
</div>
